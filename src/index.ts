/**
 * timeflare-sdk — the single TypeScript client for the Timeflare protocol.
 *
 * Generated protobuf types, REST + cosmjs chain access, protocol constants,
 * creator + recipient flows, and crypto behind a CryptoProvider interface
 * with backends compiled from the one Rust crate (WASM here for web/Node;
 * JSI in the mobile build). Everything is re-exported from ./protocol.
 */

export * from './protocol';
