// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: timeflare/secrets/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Guardian, KeyHistoryEntry } from "./guardian";
import { AssignmentRecord, RevealedShare, Secret, SecretShareData, SecretTombstone } from "./secret";

export const protobufPackage = "timeflare.secrets.v1";

/** StoredShare is a genesis entry for one guardian's encrypted share record. */
export interface StoredShare {
  secretId: string;
  guardianAddress: string;
  data: SecretShareData | undefined;
}

/** StoredAssignment is a genesis entry for one guardian's assignment status record. */
export interface StoredAssignment {
  secretId: string;
  guardianAddress: string;
  record: AssignmentRecord | undefined;
}

/**
 * StoredReveal is a genesis entry for one revealed share (the guardian
 * address is carried inside the RevealedShare).
 */
export interface StoredReveal {
  secretId: string;
  reveal: RevealedShare | undefined;
}

/**
 * EarlyRevealSlashEntry marks a guardian slashed for an early reveal on a
 * secret — required at settlement to exclude the guardian from payouts, so it
 * is genuine state, not derivable.
 */
export interface EarlyRevealSlashEntry {
  secretId: string;
  guardianAddress: string;
}

/** StoredPayload is a genesis entry for one secret's payload ciphertext. */
export interface StoredPayload {
  secretId: string;
  payloadCiphertext: Uint8Array;
}

/**
 * GenesisState defines the secrets module's genesis state. The due-height
 * queues and the creator index are derived state and are rebuilt on import.
 */
export interface GenesisState {
  /** secrets defines all the slim secret records stored at genesis. */
  secrets: Secret[];
  /** guardians defines all the guardians registered at genesis. */
  guardians: Guardian[];
  /** secret_shares defines the per-guardian encrypted share records. */
  secretShares: StoredShare[];
  /** secret_assignments defines the per-guardian assignment status records. */
  secretAssignments: StoredAssignment[];
  /** secret_reveals defines the revealed share records. */
  secretReveals: StoredReveal[];
  /**
   * early_reveal_slashes defines guardians slashed for early reveals on
   * still-live secrets.
   */
  earlyRevealSlashes: EarlyRevealSlashEntry[];
  /** secret_payloads defines the stored payload ciphertexts. */
  secretPayloads: StoredPayload[];
  /**
   * secret_counter is the monotonic sequence that assigns secret IDs and
   * differentiates selection seeds. It is consensus state and must survive
   * export/import: it only ever climbs, and is deliberately independent of
   * the stored secrets (pruned secrets consumed counter values too — never
   * re-derive it from the secret set).
   */
  secretCounter: number;
  /**
   * secret_tombstones defines the permanent tombstones of pruned secrets.
   * The prune queue is derived state: it is rebuilt on import from the
   * non-pruned terminal secrets' terminal_at + RetentionBlocks.
   */
  secretTombstones: StoredTombstone[];
  /**
   * rebate_state is the recipient-rebate accrual state: the allowance
   * available to credit, the height it was last accrued to, and the total
   * credited but not yet collected. Consensus state that must survive
   * export/import — a lost allowance would re-accrue from zero, and a lost
   * reservation would let the pool promise funds twice.
   */
  rebateState:
    | RebateState
    | undefined;
  /**
   * guardian_key_histories defines every guardian's append-only key-epoch
   * history. A guardian with no entries here gets epoch 0 synthesised from
   * its record on import (the pre-rotation migration path). The key
   * uniqueness index is derived state — rebuilt on import from the history.
   */
  guardianKeyHistories: StoredKeyHistoryEntry[];
}

/**
 * StoredKeyHistoryEntry is a genesis entry for one epoch of one guardian's
 * key history.
 */
export interface StoredKeyHistoryEntry {
  guardianAddress: string;
  epoch: number;
  entry: KeyHistoryEntry | undefined;
}

/**
 * StoredTombstone pairs a pruned secret's id with its permanent tombstone
 * for genesis export/import.
 */
export interface StoredTombstone {
  secretId: string;
  tombstone: SecretTombstone | undefined;
}

/**
 * RebateState carries the recipient-rebate mechanism's accrual bookkeeping.
 * See docs/spec.md "Recipient Rebate".
 */
export interface RebateState {
  /**
   * Allowance available to credit, in uveil. Accrues from the rebate pool's
   * balance and is capped at RebateBurstBlocks of accrual.
   */
  allowance: number;
  /**
   * Height the allowance was last accrued to. Accrual is lazy: an idle block
   * does no work, and the gap is applied the next time a settlement consumes
   * the allowance.
   */
  accruedHeight: number;
  /**
   * Total credited to secrets but not yet collected, in uveil. Reserved
   * against the pool balance so a credited rebate is always payable.
   */
  reserved: number;
}

function createBaseStoredShare(): StoredShare {
  return { secretId: "", guardianAddress: "", data: undefined };
}

export const StoredShare: MessageFns<StoredShare> = {
  encode(message: StoredShare, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.guardianAddress !== "") {
      writer.uint32(18).string(message.guardianAddress);
    }
    if (message.data !== undefined) {
      SecretShareData.encode(message.data, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredShare {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredShare();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guardianAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = SecretShareData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredShare {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      guardianAddress: isSet(object.guardianAddress)
        ? globalThis.String(object.guardianAddress)
        : isSet(object.guardian_address)
        ? globalThis.String(object.guardian_address)
        : "",
      data: isSet(object.data) ? SecretShareData.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: StoredShare): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.guardianAddress !== "") {
      obj.guardianAddress = message.guardianAddress;
    }
    if (message.data !== undefined) {
      obj.data = SecretShareData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredShare>, I>>(base?: I): StoredShare {
    return StoredShare.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredShare>, I>>(object: I): StoredShare {
    const message = createBaseStoredShare();
    message.secretId = object.secretId ?? "";
    message.guardianAddress = object.guardianAddress ?? "";
    message.data = (object.data !== undefined && object.data !== null)
      ? SecretShareData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseStoredAssignment(): StoredAssignment {
  return { secretId: "", guardianAddress: "", record: undefined };
}

export const StoredAssignment: MessageFns<StoredAssignment> = {
  encode(message: StoredAssignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.guardianAddress !== "") {
      writer.uint32(18).string(message.guardianAddress);
    }
    if (message.record !== undefined) {
      AssignmentRecord.encode(message.record, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guardianAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.record = AssignmentRecord.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredAssignment {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      guardianAddress: isSet(object.guardianAddress)
        ? globalThis.String(object.guardianAddress)
        : isSet(object.guardian_address)
        ? globalThis.String(object.guardian_address)
        : "",
      record: isSet(object.record) ? AssignmentRecord.fromJSON(object.record) : undefined,
    };
  },

  toJSON(message: StoredAssignment): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.guardianAddress !== "") {
      obj.guardianAddress = message.guardianAddress;
    }
    if (message.record !== undefined) {
      obj.record = AssignmentRecord.toJSON(message.record);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredAssignment>, I>>(base?: I): StoredAssignment {
    return StoredAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredAssignment>, I>>(object: I): StoredAssignment {
    const message = createBaseStoredAssignment();
    message.secretId = object.secretId ?? "";
    message.guardianAddress = object.guardianAddress ?? "";
    message.record = (object.record !== undefined && object.record !== null)
      ? AssignmentRecord.fromPartial(object.record)
      : undefined;
    return message;
  },
};

function createBaseStoredReveal(): StoredReveal {
  return { secretId: "", reveal: undefined };
}

export const StoredReveal: MessageFns<StoredReveal> = {
  encode(message: StoredReveal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.reveal !== undefined) {
      RevealedShare.encode(message.reveal, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredReveal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredReveal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reveal = RevealedShare.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredReveal {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      reveal: isSet(object.reveal) ? RevealedShare.fromJSON(object.reveal) : undefined,
    };
  },

  toJSON(message: StoredReveal): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.reveal !== undefined) {
      obj.reveal = RevealedShare.toJSON(message.reveal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredReveal>, I>>(base?: I): StoredReveal {
    return StoredReveal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredReveal>, I>>(object: I): StoredReveal {
    const message = createBaseStoredReveal();
    message.secretId = object.secretId ?? "";
    message.reveal = (object.reveal !== undefined && object.reveal !== null)
      ? RevealedShare.fromPartial(object.reveal)
      : undefined;
    return message;
  },
};

function createBaseEarlyRevealSlashEntry(): EarlyRevealSlashEntry {
  return { secretId: "", guardianAddress: "" };
}

export const EarlyRevealSlashEntry: MessageFns<EarlyRevealSlashEntry> = {
  encode(message: EarlyRevealSlashEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.guardianAddress !== "") {
      writer.uint32(18).string(message.guardianAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EarlyRevealSlashEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEarlyRevealSlashEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guardianAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EarlyRevealSlashEntry {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      guardianAddress: isSet(object.guardianAddress)
        ? globalThis.String(object.guardianAddress)
        : isSet(object.guardian_address)
        ? globalThis.String(object.guardian_address)
        : "",
    };
  },

  toJSON(message: EarlyRevealSlashEntry): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.guardianAddress !== "") {
      obj.guardianAddress = message.guardianAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EarlyRevealSlashEntry>, I>>(base?: I): EarlyRevealSlashEntry {
    return EarlyRevealSlashEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EarlyRevealSlashEntry>, I>>(object: I): EarlyRevealSlashEntry {
    const message = createBaseEarlyRevealSlashEntry();
    message.secretId = object.secretId ?? "";
    message.guardianAddress = object.guardianAddress ?? "";
    return message;
  },
};

function createBaseStoredPayload(): StoredPayload {
  return { secretId: "", payloadCiphertext: new Uint8Array(0) };
}

export const StoredPayload: MessageFns<StoredPayload> = {
  encode(message: StoredPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.payloadCiphertext.length !== 0) {
      writer.uint32(18).bytes(message.payloadCiphertext);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payloadCiphertext = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredPayload {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      payloadCiphertext: isSet(object.payloadCiphertext)
        ? bytesFromBase64(object.payloadCiphertext)
        : isSet(object.payload_ciphertext)
        ? bytesFromBase64(object.payload_ciphertext)
        : new Uint8Array(0),
    };
  },

  toJSON(message: StoredPayload): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.payloadCiphertext.length !== 0) {
      obj.payloadCiphertext = base64FromBytes(message.payloadCiphertext);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredPayload>, I>>(base?: I): StoredPayload {
    return StoredPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredPayload>, I>>(object: I): StoredPayload {
    const message = createBaseStoredPayload();
    message.secretId = object.secretId ?? "";
    message.payloadCiphertext = object.payloadCiphertext ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGenesisState(): GenesisState {
  return {
    secrets: [],
    guardians: [],
    secretShares: [],
    secretAssignments: [],
    secretReveals: [],
    earlyRevealSlashes: [],
    secretPayloads: [],
    secretCounter: 0,
    secretTombstones: [],
    rebateState: undefined,
    guardianKeyHistories: [],
  };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.secrets) {
      Secret.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.guardians) {
      Guardian.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.secretShares) {
      StoredShare.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.secretAssignments) {
      StoredAssignment.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.secretReveals) {
      StoredReveal.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.earlyRevealSlashes) {
      EarlyRevealSlashEntry.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.secretPayloads) {
      StoredPayload.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.secretCounter !== 0) {
      writer.uint32(64).uint64(message.secretCounter);
    }
    for (const v of message.secretTombstones) {
      StoredTombstone.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.rebateState !== undefined) {
      RebateState.encode(message.rebateState, writer.uint32(90).fork()).join();
    }
    for (const v of message.guardianKeyHistories) {
      StoredKeyHistoryEntry.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secrets.push(Secret.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guardians.push(Guardian.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.secretShares.push(StoredShare.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.secretAssignments.push(StoredAssignment.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secretReveals.push(StoredReveal.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.earlyRevealSlashes.push(EarlyRevealSlashEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.secretPayloads.push(StoredPayload.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.secretCounter = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.secretTombstones.push(StoredTombstone.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.rebateState = RebateState.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.guardianKeyHistories.push(StoredKeyHistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      secrets: globalThis.Array.isArray(object?.secrets) ? object.secrets.map((e: any) => Secret.fromJSON(e)) : [],
      guardians: globalThis.Array.isArray(object?.guardians)
        ? object.guardians.map((e: any) => Guardian.fromJSON(e))
        : [],
      secretShares: globalThis.Array.isArray(object?.secretShares)
        ? object.secretShares.map((e: any) => StoredShare.fromJSON(e))
        : globalThis.Array.isArray(object?.secret_shares)
        ? object.secret_shares.map((e: any) => StoredShare.fromJSON(e))
        : [],
      secretAssignments: globalThis.Array.isArray(object?.secretAssignments)
        ? object.secretAssignments.map((e: any) => StoredAssignment.fromJSON(e))
        : globalThis.Array.isArray(object?.secret_assignments)
        ? object.secret_assignments.map((e: any) => StoredAssignment.fromJSON(e))
        : [],
      secretReveals: globalThis.Array.isArray(object?.secretReveals)
        ? object.secretReveals.map((e: any) => StoredReveal.fromJSON(e))
        : globalThis.Array.isArray(object?.secret_reveals)
        ? object.secret_reveals.map((e: any) => StoredReveal.fromJSON(e))
        : [],
      earlyRevealSlashes: globalThis.Array.isArray(object?.earlyRevealSlashes)
        ? object.earlyRevealSlashes.map((e: any) => EarlyRevealSlashEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.early_reveal_slashes)
        ? object.early_reveal_slashes.map((e: any) => EarlyRevealSlashEntry.fromJSON(e))
        : [],
      secretPayloads: globalThis.Array.isArray(object?.secretPayloads)
        ? object.secretPayloads.map((e: any) => StoredPayload.fromJSON(e))
        : globalThis.Array.isArray(object?.secret_payloads)
        ? object.secret_payloads.map((e: any) => StoredPayload.fromJSON(e))
        : [],
      secretCounter: isSet(object.secretCounter)
        ? globalThis.Number(object.secretCounter)
        : isSet(object.secret_counter)
        ? globalThis.Number(object.secret_counter)
        : 0,
      secretTombstones: globalThis.Array.isArray(object?.secretTombstones)
        ? object.secretTombstones.map((e: any) => StoredTombstone.fromJSON(e))
        : globalThis.Array.isArray(object?.secret_tombstones)
        ? object.secret_tombstones.map((e: any) => StoredTombstone.fromJSON(e))
        : [],
      rebateState: isSet(object.rebateState)
        ? RebateState.fromJSON(object.rebateState)
        : isSet(object.rebate_state)
        ? RebateState.fromJSON(object.rebate_state)
        : undefined,
      guardianKeyHistories: globalThis.Array.isArray(object?.guardianKeyHistories)
        ? object.guardianKeyHistories.map((e: any) => StoredKeyHistoryEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.guardian_key_histories)
        ? object.guardian_key_histories.map((e: any) => StoredKeyHistoryEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.secrets?.length) {
      obj.secrets = message.secrets.map((e) => Secret.toJSON(e));
    }
    if (message.guardians?.length) {
      obj.guardians = message.guardians.map((e) => Guardian.toJSON(e));
    }
    if (message.secretShares?.length) {
      obj.secretShares = message.secretShares.map((e) => StoredShare.toJSON(e));
    }
    if (message.secretAssignments?.length) {
      obj.secretAssignments = message.secretAssignments.map((e) => StoredAssignment.toJSON(e));
    }
    if (message.secretReveals?.length) {
      obj.secretReveals = message.secretReveals.map((e) => StoredReveal.toJSON(e));
    }
    if (message.earlyRevealSlashes?.length) {
      obj.earlyRevealSlashes = message.earlyRevealSlashes.map((e) => EarlyRevealSlashEntry.toJSON(e));
    }
    if (message.secretPayloads?.length) {
      obj.secretPayloads = message.secretPayloads.map((e) => StoredPayload.toJSON(e));
    }
    if (message.secretCounter !== 0) {
      obj.secretCounter = Math.round(message.secretCounter);
    }
    if (message.secretTombstones?.length) {
      obj.secretTombstones = message.secretTombstones.map((e) => StoredTombstone.toJSON(e));
    }
    if (message.rebateState !== undefined) {
      obj.rebateState = RebateState.toJSON(message.rebateState);
    }
    if (message.guardianKeyHistories?.length) {
      obj.guardianKeyHistories = message.guardianKeyHistories.map((e) => StoredKeyHistoryEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.secrets = object.secrets?.map((e) => Secret.fromPartial(e)) || [];
    message.guardians = object.guardians?.map((e) => Guardian.fromPartial(e)) || [];
    message.secretShares = object.secretShares?.map((e) => StoredShare.fromPartial(e)) || [];
    message.secretAssignments = object.secretAssignments?.map((e) => StoredAssignment.fromPartial(e)) || [];
    message.secretReveals = object.secretReveals?.map((e) => StoredReveal.fromPartial(e)) || [];
    message.earlyRevealSlashes = object.earlyRevealSlashes?.map((e) => EarlyRevealSlashEntry.fromPartial(e)) || [];
    message.secretPayloads = object.secretPayloads?.map((e) => StoredPayload.fromPartial(e)) || [];
    message.secretCounter = object.secretCounter ?? 0;
    message.secretTombstones = object.secretTombstones?.map((e) => StoredTombstone.fromPartial(e)) || [];
    message.rebateState = (object.rebateState !== undefined && object.rebateState !== null)
      ? RebateState.fromPartial(object.rebateState)
      : undefined;
    message.guardianKeyHistories = object.guardianKeyHistories?.map((e) => StoredKeyHistoryEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStoredKeyHistoryEntry(): StoredKeyHistoryEntry {
  return { guardianAddress: "", epoch: 0, entry: undefined };
}

export const StoredKeyHistoryEntry: MessageFns<StoredKeyHistoryEntry> = {
  encode(message: StoredKeyHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guardianAddress !== "") {
      writer.uint32(10).string(message.guardianAddress);
    }
    if (message.epoch !== 0) {
      writer.uint32(16).uint64(message.epoch);
    }
    if (message.entry !== undefined) {
      KeyHistoryEntry.encode(message.entry, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredKeyHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredKeyHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guardianAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.entry = KeyHistoryEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredKeyHistoryEntry {
    return {
      guardianAddress: isSet(object.guardianAddress)
        ? globalThis.String(object.guardianAddress)
        : isSet(object.guardian_address)
        ? globalThis.String(object.guardian_address)
        : "",
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      entry: isSet(object.entry) ? KeyHistoryEntry.fromJSON(object.entry) : undefined,
    };
  },

  toJSON(message: StoredKeyHistoryEntry): unknown {
    const obj: any = {};
    if (message.guardianAddress !== "") {
      obj.guardianAddress = message.guardianAddress;
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.entry !== undefined) {
      obj.entry = KeyHistoryEntry.toJSON(message.entry);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredKeyHistoryEntry>, I>>(base?: I): StoredKeyHistoryEntry {
    return StoredKeyHistoryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredKeyHistoryEntry>, I>>(object: I): StoredKeyHistoryEntry {
    const message = createBaseStoredKeyHistoryEntry();
    message.guardianAddress = object.guardianAddress ?? "";
    message.epoch = object.epoch ?? 0;
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? KeyHistoryEntry.fromPartial(object.entry)
      : undefined;
    return message;
  },
};

function createBaseStoredTombstone(): StoredTombstone {
  return { secretId: "", tombstone: undefined };
}

export const StoredTombstone: MessageFns<StoredTombstone> = {
  encode(message: StoredTombstone, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.tombstone !== undefined) {
      SecretTombstone.encode(message.tombstone, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredTombstone {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredTombstone();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tombstone = SecretTombstone.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredTombstone {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      tombstone: isSet(object.tombstone) ? SecretTombstone.fromJSON(object.tombstone) : undefined,
    };
  },

  toJSON(message: StoredTombstone): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.tombstone !== undefined) {
      obj.tombstone = SecretTombstone.toJSON(message.tombstone);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredTombstone>, I>>(base?: I): StoredTombstone {
    return StoredTombstone.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredTombstone>, I>>(object: I): StoredTombstone {
    const message = createBaseStoredTombstone();
    message.secretId = object.secretId ?? "";
    message.tombstone = (object.tombstone !== undefined && object.tombstone !== null)
      ? SecretTombstone.fromPartial(object.tombstone)
      : undefined;
    return message;
  },
};

function createBaseRebateState(): RebateState {
  return { allowance: 0, accruedHeight: 0, reserved: 0 };
}

export const RebateState: MessageFns<RebateState> = {
  encode(message: RebateState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allowance !== 0) {
      writer.uint32(8).int64(message.allowance);
    }
    if (message.accruedHeight !== 0) {
      writer.uint32(16).int64(message.accruedHeight);
    }
    if (message.reserved !== 0) {
      writer.uint32(24).int64(message.reserved);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RebateState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRebateState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.allowance = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accruedHeight = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reserved = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RebateState {
    return {
      allowance: isSet(object.allowance) ? globalThis.Number(object.allowance) : 0,
      accruedHeight: isSet(object.accruedHeight)
        ? globalThis.Number(object.accruedHeight)
        : isSet(object.accrued_height)
        ? globalThis.Number(object.accrued_height)
        : 0,
      reserved: isSet(object.reserved) ? globalThis.Number(object.reserved) : 0,
    };
  },

  toJSON(message: RebateState): unknown {
    const obj: any = {};
    if (message.allowance !== 0) {
      obj.allowance = Math.round(message.allowance);
    }
    if (message.accruedHeight !== 0) {
      obj.accruedHeight = Math.round(message.accruedHeight);
    }
    if (message.reserved !== 0) {
      obj.reserved = Math.round(message.reserved);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RebateState>, I>>(base?: I): RebateState {
    return RebateState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RebateState>, I>>(object: I): RebateState {
    const message = createBaseRebateState();
    message.allowance = object.allowance ?? 0;
    message.accruedHeight = object.accruedHeight ?? 0;
    message.reserved = object.reserved ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
