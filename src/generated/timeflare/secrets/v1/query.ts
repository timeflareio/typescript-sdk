// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: timeflare/secrets/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination";
import { Coin } from "../../../cosmos/base/v1beta1/coin";
import { Guardian, KeyHistoryEntry } from "./guardian";
import {
  AssignmentStatus,
  assignmentStatusFromJSON,
  assignmentStatusToJSON,
  DetectionHint,
  GuardianAssignment,
  RevealedShare,
  Secret,
  SecretShareData,
  SecretTombstone,
} from "./secret";

export const protobufPackage = "timeflare.secrets.v1";

/**
 * SecretView is the assembled query shape: the slim stored Secret joined with
 * its side-stores (assignments, reveals). Field numbers 1-20 deliberately
 * match the pre-split Secret message so existing clients decode responses
 * unchanged; field 9 (guardian_selection) is retired — selection inputs are
 * no longer stored (see the reservation event and tx history).
 */
export interface SecretView {
  id: string;
  creator: string;
  detectionHint: DetectionHint | undefined;
  revealStartBlock: number;
  revealEndBlock: number;
  threshold: number;
  secretCommitment: Uint8Array;
  commitDeadline: number;
  state: string;
  /** Assembled from the SecretShareData + AssignmentRecord side-stores */
  guardianAssignments: GuardianAssignment[];
  /** The accepted (= active) guardian addresses */
  activeAssignments: string[];
  revealedShares: RevealedShare[];
  rewardPool:
    | Coin
    | undefined;
  /**
   * The acceptance reimbursement escrowed apart from the pool
   * (A = max_shares × F_accept), settled at the secret's terminal state to
   * the guardians that did the job asked of them
   */
  acceptFees: Coin | undefined;
  createdAt: number;
  bump: number;
  selectedGuardians: string[];
  acceptedCount: number;
  revealedCount: number;
  terminalAt: number;
  /**
   * pk_s — joins the view additively; the payload ciphertext itself is served
   * only by the granular SecretPayload endpoint
   */
  secretPublicKey: Uint8Array;
  /**
   * Per-guardian bond amounts in uveil, aligned with selected_guardians
   * (frozen at selection — see Secret.guardian_bond_amounts)
   */
  guardianBondAmounts: number[];
  /** Band floor (threshold ≤ min_shares) — see Secret.min_shares */
  minShares: number;
  /** Band ceiling (min ≤ max ≤ 32, max − min < threshold) — see Secret.max_shares */
  maxShares: number;
  /**
   * Rebate credited to this secret's recipient at settlement, in uveil, and
   * reserved in the keyless rebate pool until collected. Zero means none was
   * credited: the secret did not reach `revealed`, or its share fell below the
   * dust floor. See docs/spec.md "Recipient Rebate".
   */
  rebateAmount: number;
  /**
   * True once the rebate has been collected. A positive amount that is not yet
   * collected is the only collectable condition.
   */
  rebateCollected: boolean;
}

/** SecretAssignmentView is one guardian's assignment status (no share bytes). */
export interface SecretAssignmentView {
  guardianAddress: string;
  status: AssignmentStatus;
  respondedAtBlock: number;
  /** Whether an encrypted share was distributed to this guardian */
  hasShare: boolean;
}

/** QuerySecretRequest is the request type for the Query/Secret RPC method. */
export interface QuerySecretRequest {
  secretId: string;
}

/** QuerySecretResponse is the response type for the Query/Secret RPC method. */
export interface QuerySecretResponse {
  secret: SecretView | undefined;
}

/** QuerySecretsRequest is the request type for the Query/Secrets RPC method. */
export interface QuerySecretsRequest {
  pagination: PageRequest | undefined;
}

/** QuerySecretsResponse is the response type for the Query/Secrets RPC method. */
export interface QuerySecretsResponse {
  secrets: SecretView[];
  pagination: PageResponse | undefined;
}

/** QuerySecretsByCreatorRequest is the request type for the Query/SecretsByCreator RPC method. */
export interface QuerySecretsByCreatorRequest {
  creator: string;
  pagination: PageRequest | undefined;
}

/** QuerySecretsByCreatorResponse is the response type for the Query/SecretsByCreator RPC method. */
export interface QuerySecretsByCreatorResponse {
  secrets: SecretView[];
  pagination: PageResponse | undefined;
}

/** QueryPendingSecretsRequest is the request type for the Query/PendingSecrets RPC method. */
export interface QueryPendingSecretsRequest {
  pagination: PageRequest | undefined;
}

/** QueryPendingSecretsResponse is the response type for the Query/PendingSecrets RPC method. */
export interface QueryPendingSecretsResponse {
  secrets: SecretView[];
  pagination: PageResponse | undefined;
}

/** QuerySecretMetaRequest is the request type for the Query/SecretMeta RPC method. */
export interface QuerySecretMetaRequest {
  secretId: string;
}

/** QuerySecretMetaResponse is the response type for the Query/SecretMeta RPC method. */
export interface QuerySecretMetaResponse {
  secret: Secret | undefined;
}

/** QuerySecretAssignmentsRequest is the request type for the Query/SecretAssignments RPC method. */
export interface QuerySecretAssignmentsRequest {
  secretId: string;
}

/** QuerySecretAssignmentsResponse is the response type for the Query/SecretAssignments RPC method. */
export interface QuerySecretAssignmentsResponse {
  assignments: SecretAssignmentView[];
}

/** QuerySecretRevealsRequest is the request type for the Query/SecretReveals RPC method. */
export interface QuerySecretRevealsRequest {
  secretId: string;
}

/** QuerySecretRevealsResponse is the response type for the Query/SecretReveals RPC method. */
export interface QuerySecretRevealsResponse {
  reveals: RevealedShare[];
}

/** QuerySecretShareRequest is the request type for the Query/SecretShare RPC method. */
export interface QuerySecretShareRequest {
  secretId: string;
  guardianAddress: string;
}

/** QuerySecretShareResponse is the response type for the Query/SecretShare RPC method. */
export interface QuerySecretShareResponse {
  share: SecretShareData | undefined;
}

/** QuerySecretPayloadRequest is the request type for the Query/SecretPayload RPC method. */
export interface QuerySecretPayloadRequest {
  secretId: string;
}

/** QuerySecretPayloadResponse is the response type for the Query/SecretPayload RPC method. */
export interface QuerySecretPayloadResponse {
  /** The payload ciphertext C (outer-encrypted to the per-secret key) */
  payloadCiphertext: Uint8Array;
}

/** QuerySecretTombstoneRequest is the request type for the Query/SecretTombstone RPC method. */
export interface QuerySecretTombstoneRequest {
  secretId: string;
}

/** QuerySecretTombstoneResponse is the response type for the Query/SecretTombstone RPC method. */
export interface QuerySecretTombstoneResponse {
  tombstone: SecretTombstone | undefined;
}

/**
 * QueryHintsSinceRequest is the request type for the Query/HintsSince RPC
 * method. since_height is inclusive; pagination applies within the range.
 */
export interface QueryHintsSinceRequest {
  sinceHeight: number;
  pagination: PageRequest | undefined;
}

/** HintRecord is one compact discovery-scan entry (~50B). */
export interface HintRecord {
  secretId: string;
  createdAt: number;
  detectionHint: DetectionHint | undefined;
}

/**
 * QueryHintsSinceResponse is the response type for the Query/HintsSince RPC
 * method. Records are returned in (created_at, secret_id) order so clients
 * can persist the last created_at as their resume cursor.
 */
export interface QueryHintsSinceResponse {
  hints: HintRecord[];
  pagination: PageResponse | undefined;
}

/** QueryGuardianRequest is request type for the Query/Guardian RPC method. */
export interface QueryGuardianRequest {
  address: string;
}

/** QueryGuardianResponse is response type for the Query/Guardian RPC method. */
export interface QueryGuardianResponse {
  guardian: Guardian | undefined;
}

/** QueryGuardiansRequest is request type for the Query/Guardians RPC method. */
export interface QueryGuardiansRequest {
  pagination: PageRequest | undefined;
}

/** QueryGuardiansResponse is response type for the Query/Guardians RPC method. */
export interface QueryGuardiansResponse {
  guardians: Guardian[];
  pagination: PageResponse | undefined;
}

/**
 * GuardianKeyEpoch is one entry of a guardian's key history, with its epoch
 * number made explicit for clients.
 */
export interface GuardianKeyEpoch {
  epoch: number;
  entry: KeyHistoryEntry | undefined;
}

/**
 * QueryGuardianKeyHistoryRequest is request type for the
 * Query/GuardianKeyHistory RPC method.
 */
export interface QueryGuardianKeyHistoryRequest {
  address: string;
}

/**
 * QueryGuardianKeyHistoryResponse is response type for the
 * Query/GuardianKeyHistory RPC method. Entries are returned in epoch order
 * (epoch 0 = the registration key); histories are small by construction
 * (the rotation interval bounds growth to ~12 entries per guardian-year),
 * so the response is unpaginated.
 */
export interface QueryGuardianKeyHistoryResponse {
  epochs: GuardianKeyEpoch[];
}

function createBaseSecretView(): SecretView {
  return {
    id: "",
    creator: "",
    detectionHint: undefined,
    revealStartBlock: 0,
    revealEndBlock: 0,
    threshold: 0,
    secretCommitment: new Uint8Array(0),
    commitDeadline: 0,
    state: "",
    guardianAssignments: [],
    activeAssignments: [],
    revealedShares: [],
    rewardPool: undefined,
    acceptFees: undefined,
    createdAt: 0,
    bump: 0,
    selectedGuardians: [],
    acceptedCount: 0,
    revealedCount: 0,
    terminalAt: 0,
    secretPublicKey: new Uint8Array(0),
    guardianBondAmounts: [],
    minShares: 0,
    maxShares: 0,
    rebateAmount: 0,
    rebateCollected: false,
  };
}

export const SecretView: MessageFns<SecretView> = {
  encode(message: SecretView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.creator !== "") {
      writer.uint32(18).string(message.creator);
    }
    if (message.detectionHint !== undefined) {
      DetectionHint.encode(message.detectionHint, writer.uint32(26).fork()).join();
    }
    if (message.revealStartBlock !== 0) {
      writer.uint32(40).int64(message.revealStartBlock);
    }
    if (message.revealEndBlock !== 0) {
      writer.uint32(48).int64(message.revealEndBlock);
    }
    if (message.threshold !== 0) {
      writer.uint32(56).int64(message.threshold);
    }
    if (message.secretCommitment.length !== 0) {
      writer.uint32(82).bytes(message.secretCommitment);
    }
    if (message.commitDeadline !== 0) {
      writer.uint32(88).int64(message.commitDeadline);
    }
    if (message.state !== "") {
      writer.uint32(98).string(message.state);
    }
    for (const v of message.guardianAssignments) {
      GuardianAssignment.encode(v!, writer.uint32(106).fork()).join();
    }
    for (const v of message.activeAssignments) {
      writer.uint32(114).string(v!);
    }
    for (const v of message.revealedShares) {
      RevealedShare.encode(v!, writer.uint32(122).fork()).join();
    }
    if (message.rewardPool !== undefined) {
      Coin.encode(message.rewardPool, writer.uint32(130).fork()).join();
    }
    if (message.acceptFees !== undefined) {
      Coin.encode(message.acceptFees, writer.uint32(234).fork()).join();
    }
    if (message.createdAt !== 0) {
      writer.uint32(136).int64(message.createdAt);
    }
    if (message.bump !== 0) {
      writer.uint32(152).int64(message.bump);
    }
    for (const v of message.selectedGuardians) {
      writer.uint32(170).string(v!);
    }
    if (message.acceptedCount !== 0) {
      writer.uint32(176).int64(message.acceptedCount);
    }
    if (message.revealedCount !== 0) {
      writer.uint32(184).int64(message.revealedCount);
    }
    if (message.terminalAt !== 0) {
      writer.uint32(192).int64(message.terminalAt);
    }
    if (message.secretPublicKey.length !== 0) {
      writer.uint32(202).bytes(message.secretPublicKey);
    }
    writer.uint32(210).fork();
    for (const v of message.guardianBondAmounts) {
      writer.int64(v);
    }
    writer.join();
    if (message.minShares !== 0) {
      writer.uint32(216).int64(message.minShares);
    }
    if (message.maxShares !== 0) {
      writer.uint32(224).int64(message.maxShares);
    }
    if (message.rebateAmount !== 0) {
      writer.uint32(240).int64(message.rebateAmount);
    }
    if (message.rebateCollected !== false) {
      writer.uint32(248).bool(message.rebateCollected);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecretView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecretView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.detectionHint = DetectionHint.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.revealStartBlock = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.revealEndBlock = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.threshold = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.secretCommitment = reader.bytes();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.commitDeadline = longToNumber(reader.int64());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.guardianAssignments.push(GuardianAssignment.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.activeAssignments.push(reader.string());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.revealedShares.push(RevealedShare.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.rewardPool = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.acceptFees = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 19: {
          if (tag !== 152) {
            break;
          }

          message.bump = longToNumber(reader.int64());
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.selectedGuardians.push(reader.string());
          continue;
        }
        case 22: {
          if (tag !== 176) {
            break;
          }

          message.acceptedCount = longToNumber(reader.int64());
          continue;
        }
        case 23: {
          if (tag !== 184) {
            break;
          }

          message.revealedCount = longToNumber(reader.int64());
          continue;
        }
        case 24: {
          if (tag !== 192) {
            break;
          }

          message.terminalAt = longToNumber(reader.int64());
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.secretPublicKey = reader.bytes();
          continue;
        }
        case 26: {
          if (tag === 208) {
            message.guardianBondAmounts.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 210) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.guardianBondAmounts.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
        case 27: {
          if (tag !== 216) {
            break;
          }

          message.minShares = longToNumber(reader.int64());
          continue;
        }
        case 28: {
          if (tag !== 224) {
            break;
          }

          message.maxShares = longToNumber(reader.int64());
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.rebateAmount = longToNumber(reader.int64());
          continue;
        }
        case 31: {
          if (tag !== 248) {
            break;
          }

          message.rebateCollected = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecretView {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      detectionHint: isSet(object.detectionHint)
        ? DetectionHint.fromJSON(object.detectionHint)
        : isSet(object.detection_hint)
        ? DetectionHint.fromJSON(object.detection_hint)
        : undefined,
      revealStartBlock: isSet(object.revealStartBlock)
        ? globalThis.Number(object.revealStartBlock)
        : isSet(object.reveal_start_block)
        ? globalThis.Number(object.reveal_start_block)
        : 0,
      revealEndBlock: isSet(object.revealEndBlock)
        ? globalThis.Number(object.revealEndBlock)
        : isSet(object.reveal_end_block)
        ? globalThis.Number(object.reveal_end_block)
        : 0,
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
      secretCommitment: isSet(object.secretCommitment)
        ? bytesFromBase64(object.secretCommitment)
        : isSet(object.secret_commitment)
        ? bytesFromBase64(object.secret_commitment)
        : new Uint8Array(0),
      commitDeadline: isSet(object.commitDeadline)
        ? globalThis.Number(object.commitDeadline)
        : isSet(object.commit_deadline)
        ? globalThis.Number(object.commit_deadline)
        : 0,
      state: isSet(object.state) ? globalThis.String(object.state) : "",
      guardianAssignments: globalThis.Array.isArray(object?.guardianAssignments)
        ? object.guardianAssignments.map((e: any) => GuardianAssignment.fromJSON(e))
        : globalThis.Array.isArray(object?.guardian_assignments)
        ? object.guardian_assignments.map((e: any) => GuardianAssignment.fromJSON(e))
        : [],
      activeAssignments: globalThis.Array.isArray(object?.activeAssignments)
        ? object.activeAssignments.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.active_assignments)
        ? object.active_assignments.map((e: any) => globalThis.String(e))
        : [],
      revealedShares: globalThis.Array.isArray(object?.revealedShares)
        ? object.revealedShares.map((e: any) => RevealedShare.fromJSON(e))
        : globalThis.Array.isArray(object?.revealed_shares)
        ? object.revealed_shares.map((e: any) => RevealedShare.fromJSON(e))
        : [],
      rewardPool: isSet(object.rewardPool)
        ? Coin.fromJSON(object.rewardPool)
        : isSet(object.reward_pool)
        ? Coin.fromJSON(object.reward_pool)
        : undefined,
      acceptFees: isSet(object.acceptFees)
        ? Coin.fromJSON(object.acceptFees)
        : isSet(object.accept_fees)
        ? Coin.fromJSON(object.accept_fees)
        : undefined,
      createdAt: isSet(object.createdAt)
        ? globalThis.Number(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.Number(object.created_at)
        : 0,
      bump: isSet(object.bump) ? globalThis.Number(object.bump) : 0,
      selectedGuardians: globalThis.Array.isArray(object?.selectedGuardians)
        ? object.selectedGuardians.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.selected_guardians)
        ? object.selected_guardians.map((e: any) => globalThis.String(e))
        : [],
      acceptedCount: isSet(object.acceptedCount)
        ? globalThis.Number(object.acceptedCount)
        : isSet(object.accepted_count)
        ? globalThis.Number(object.accepted_count)
        : 0,
      revealedCount: isSet(object.revealedCount)
        ? globalThis.Number(object.revealedCount)
        : isSet(object.revealed_count)
        ? globalThis.Number(object.revealed_count)
        : 0,
      terminalAt: isSet(object.terminalAt)
        ? globalThis.Number(object.terminalAt)
        : isSet(object.terminal_at)
        ? globalThis.Number(object.terminal_at)
        : 0,
      secretPublicKey: isSet(object.secretPublicKey)
        ? bytesFromBase64(object.secretPublicKey)
        : isSet(object.secret_public_key)
        ? bytesFromBase64(object.secret_public_key)
        : new Uint8Array(0),
      guardianBondAmounts: globalThis.Array.isArray(object?.guardianBondAmounts)
        ? object.guardianBondAmounts.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.guardian_bond_amounts)
        ? object.guardian_bond_amounts.map((e: any) => globalThis.Number(e))
        : [],
      minShares: isSet(object.minShares)
        ? globalThis.Number(object.minShares)
        : isSet(object.min_shares)
        ? globalThis.Number(object.min_shares)
        : 0,
      maxShares: isSet(object.maxShares)
        ? globalThis.Number(object.maxShares)
        : isSet(object.max_shares)
        ? globalThis.Number(object.max_shares)
        : 0,
      rebateAmount: isSet(object.rebateAmount)
        ? globalThis.Number(object.rebateAmount)
        : isSet(object.rebate_amount)
        ? globalThis.Number(object.rebate_amount)
        : 0,
      rebateCollected: isSet(object.rebateCollected)
        ? globalThis.Boolean(object.rebateCollected)
        : isSet(object.rebate_collected)
        ? globalThis.Boolean(object.rebate_collected)
        : false,
    };
  },

  toJSON(message: SecretView): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.detectionHint !== undefined) {
      obj.detectionHint = DetectionHint.toJSON(message.detectionHint);
    }
    if (message.revealStartBlock !== 0) {
      obj.revealStartBlock = Math.round(message.revealStartBlock);
    }
    if (message.revealEndBlock !== 0) {
      obj.revealEndBlock = Math.round(message.revealEndBlock);
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.secretCommitment.length !== 0) {
      obj.secretCommitment = base64FromBytes(message.secretCommitment);
    }
    if (message.commitDeadline !== 0) {
      obj.commitDeadline = Math.round(message.commitDeadline);
    }
    if (message.state !== "") {
      obj.state = message.state;
    }
    if (message.guardianAssignments?.length) {
      obj.guardianAssignments = message.guardianAssignments.map((e) => GuardianAssignment.toJSON(e));
    }
    if (message.activeAssignments?.length) {
      obj.activeAssignments = message.activeAssignments;
    }
    if (message.revealedShares?.length) {
      obj.revealedShares = message.revealedShares.map((e) => RevealedShare.toJSON(e));
    }
    if (message.rewardPool !== undefined) {
      obj.rewardPool = Coin.toJSON(message.rewardPool);
    }
    if (message.acceptFees !== undefined) {
      obj.acceptFees = Coin.toJSON(message.acceptFees);
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.bump !== 0) {
      obj.bump = Math.round(message.bump);
    }
    if (message.selectedGuardians?.length) {
      obj.selectedGuardians = message.selectedGuardians;
    }
    if (message.acceptedCount !== 0) {
      obj.acceptedCount = Math.round(message.acceptedCount);
    }
    if (message.revealedCount !== 0) {
      obj.revealedCount = Math.round(message.revealedCount);
    }
    if (message.terminalAt !== 0) {
      obj.terminalAt = Math.round(message.terminalAt);
    }
    if (message.secretPublicKey.length !== 0) {
      obj.secretPublicKey = base64FromBytes(message.secretPublicKey);
    }
    if (message.guardianBondAmounts?.length) {
      obj.guardianBondAmounts = message.guardianBondAmounts.map((e) => Math.round(e));
    }
    if (message.minShares !== 0) {
      obj.minShares = Math.round(message.minShares);
    }
    if (message.maxShares !== 0) {
      obj.maxShares = Math.round(message.maxShares);
    }
    if (message.rebateAmount !== 0) {
      obj.rebateAmount = Math.round(message.rebateAmount);
    }
    if (message.rebateCollected !== false) {
      obj.rebateCollected = message.rebateCollected;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SecretView>, I>>(base?: I): SecretView {
    return SecretView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecretView>, I>>(object: I): SecretView {
    const message = createBaseSecretView();
    message.id = object.id ?? "";
    message.creator = object.creator ?? "";
    message.detectionHint = (object.detectionHint !== undefined && object.detectionHint !== null)
      ? DetectionHint.fromPartial(object.detectionHint)
      : undefined;
    message.revealStartBlock = object.revealStartBlock ?? 0;
    message.revealEndBlock = object.revealEndBlock ?? 0;
    message.threshold = object.threshold ?? 0;
    message.secretCommitment = object.secretCommitment ?? new Uint8Array(0);
    message.commitDeadline = object.commitDeadline ?? 0;
    message.state = object.state ?? "";
    message.guardianAssignments = object.guardianAssignments?.map((e) => GuardianAssignment.fromPartial(e)) || [];
    message.activeAssignments = object.activeAssignments?.map((e) => e) || [];
    message.revealedShares = object.revealedShares?.map((e) => RevealedShare.fromPartial(e)) || [];
    message.rewardPool = (object.rewardPool !== undefined && object.rewardPool !== null)
      ? Coin.fromPartial(object.rewardPool)
      : undefined;
    message.acceptFees = (object.acceptFees !== undefined && object.acceptFees !== null)
      ? Coin.fromPartial(object.acceptFees)
      : undefined;
    message.createdAt = object.createdAt ?? 0;
    message.bump = object.bump ?? 0;
    message.selectedGuardians = object.selectedGuardians?.map((e) => e) || [];
    message.acceptedCount = object.acceptedCount ?? 0;
    message.revealedCount = object.revealedCount ?? 0;
    message.terminalAt = object.terminalAt ?? 0;
    message.secretPublicKey = object.secretPublicKey ?? new Uint8Array(0);
    message.guardianBondAmounts = object.guardianBondAmounts?.map((e) => e) || [];
    message.minShares = object.minShares ?? 0;
    message.maxShares = object.maxShares ?? 0;
    message.rebateAmount = object.rebateAmount ?? 0;
    message.rebateCollected = object.rebateCollected ?? false;
    return message;
  },
};

function createBaseSecretAssignmentView(): SecretAssignmentView {
  return { guardianAddress: "", status: 0, respondedAtBlock: 0, hasShare: false };
}

export const SecretAssignmentView: MessageFns<SecretAssignmentView> = {
  encode(message: SecretAssignmentView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guardianAddress !== "") {
      writer.uint32(10).string(message.guardianAddress);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.respondedAtBlock !== 0) {
      writer.uint32(24).int64(message.respondedAtBlock);
    }
    if (message.hasShare !== false) {
      writer.uint32(32).bool(message.hasShare);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecretAssignmentView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecretAssignmentView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guardianAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.respondedAtBlock = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hasShare = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecretAssignmentView {
    return {
      guardianAddress: isSet(object.guardianAddress)
        ? globalThis.String(object.guardianAddress)
        : isSet(object.guardian_address)
        ? globalThis.String(object.guardian_address)
        : "",
      status: isSet(object.status) ? assignmentStatusFromJSON(object.status) : 0,
      respondedAtBlock: isSet(object.respondedAtBlock)
        ? globalThis.Number(object.respondedAtBlock)
        : isSet(object.responded_at_block)
        ? globalThis.Number(object.responded_at_block)
        : 0,
      hasShare: isSet(object.hasShare)
        ? globalThis.Boolean(object.hasShare)
        : isSet(object.has_share)
        ? globalThis.Boolean(object.has_share)
        : false,
    };
  },

  toJSON(message: SecretAssignmentView): unknown {
    const obj: any = {};
    if (message.guardianAddress !== "") {
      obj.guardianAddress = message.guardianAddress;
    }
    if (message.status !== 0) {
      obj.status = assignmentStatusToJSON(message.status);
    }
    if (message.respondedAtBlock !== 0) {
      obj.respondedAtBlock = Math.round(message.respondedAtBlock);
    }
    if (message.hasShare !== false) {
      obj.hasShare = message.hasShare;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SecretAssignmentView>, I>>(base?: I): SecretAssignmentView {
    return SecretAssignmentView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecretAssignmentView>, I>>(object: I): SecretAssignmentView {
    const message = createBaseSecretAssignmentView();
    message.guardianAddress = object.guardianAddress ?? "";
    message.status = object.status ?? 0;
    message.respondedAtBlock = object.respondedAtBlock ?? 0;
    message.hasShare = object.hasShare ?? false;
    return message;
  },
};

function createBaseQuerySecretRequest(): QuerySecretRequest {
  return { secretId: "" };
}

export const QuerySecretRequest: MessageFns<QuerySecretRequest> = {
  encode(message: QuerySecretRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
    };
  },

  toJSON(message: QuerySecretRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretRequest>, I>>(base?: I): QuerySecretRequest {
    return QuerySecretRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretRequest>, I>>(object: I): QuerySecretRequest {
    const message = createBaseQuerySecretRequest();
    message.secretId = object.secretId ?? "";
    return message;
  },
};

function createBaseQuerySecretResponse(): QuerySecretResponse {
  return { secret: undefined };
}

export const QuerySecretResponse: MessageFns<QuerySecretResponse> = {
  encode(message: QuerySecretResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secret !== undefined) {
      SecretView.encode(message.secret, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secret = SecretView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretResponse {
    return { secret: isSet(object.secret) ? SecretView.fromJSON(object.secret) : undefined };
  },

  toJSON(message: QuerySecretResponse): unknown {
    const obj: any = {};
    if (message.secret !== undefined) {
      obj.secret = SecretView.toJSON(message.secret);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretResponse>, I>>(base?: I): QuerySecretResponse {
    return QuerySecretResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretResponse>, I>>(object: I): QuerySecretResponse {
    const message = createBaseQuerySecretResponse();
    message.secret = (object.secret !== undefined && object.secret !== null)
      ? SecretView.fromPartial(object.secret)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretsRequest(): QuerySecretsRequest {
  return { pagination: undefined };
}

export const QuerySecretsRequest: MessageFns<QuerySecretsRequest> = {
  encode(message: QuerySecretsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretsRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QuerySecretsRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretsRequest>, I>>(base?: I): QuerySecretsRequest {
    return QuerySecretsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretsRequest>, I>>(object: I): QuerySecretsRequest {
    const message = createBaseQuerySecretsRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretsResponse(): QuerySecretsResponse {
  return { secrets: [], pagination: undefined };
}

export const QuerySecretsResponse: MessageFns<QuerySecretsResponse> = {
  encode(message: QuerySecretsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.secrets) {
      SecretView.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secrets.push(SecretView.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretsResponse {
    return {
      secrets: globalThis.Array.isArray(object?.secrets) ? object.secrets.map((e: any) => SecretView.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QuerySecretsResponse): unknown {
    const obj: any = {};
    if (message.secrets?.length) {
      obj.secrets = message.secrets.map((e) => SecretView.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretsResponse>, I>>(base?: I): QuerySecretsResponse {
    return QuerySecretsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretsResponse>, I>>(object: I): QuerySecretsResponse {
    const message = createBaseQuerySecretsResponse();
    message.secrets = object.secrets?.map((e) => SecretView.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretsByCreatorRequest(): QuerySecretsByCreatorRequest {
  return { creator: "", pagination: undefined };
}

export const QuerySecretsByCreatorRequest: MessageFns<QuerySecretsByCreatorRequest> = {
  encode(message: QuerySecretsByCreatorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretsByCreatorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretsByCreatorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretsByCreatorRequest {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QuerySecretsByCreatorRequest): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretsByCreatorRequest>, I>>(base?: I): QuerySecretsByCreatorRequest {
    return QuerySecretsByCreatorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretsByCreatorRequest>, I>>(object: I): QuerySecretsByCreatorRequest {
    const message = createBaseQuerySecretsByCreatorRequest();
    message.creator = object.creator ?? "";
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretsByCreatorResponse(): QuerySecretsByCreatorResponse {
  return { secrets: [], pagination: undefined };
}

export const QuerySecretsByCreatorResponse: MessageFns<QuerySecretsByCreatorResponse> = {
  encode(message: QuerySecretsByCreatorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.secrets) {
      SecretView.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretsByCreatorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretsByCreatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secrets.push(SecretView.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretsByCreatorResponse {
    return {
      secrets: globalThis.Array.isArray(object?.secrets) ? object.secrets.map((e: any) => SecretView.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QuerySecretsByCreatorResponse): unknown {
    const obj: any = {};
    if (message.secrets?.length) {
      obj.secrets = message.secrets.map((e) => SecretView.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretsByCreatorResponse>, I>>(base?: I): QuerySecretsByCreatorResponse {
    return QuerySecretsByCreatorResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretsByCreatorResponse>, I>>(
    object: I,
  ): QuerySecretsByCreatorResponse {
    const message = createBaseQuerySecretsByCreatorResponse();
    message.secrets = object.secrets?.map((e) => SecretView.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryPendingSecretsRequest(): QueryPendingSecretsRequest {
  return { pagination: undefined };
}

export const QueryPendingSecretsRequest: MessageFns<QueryPendingSecretsRequest> = {
  encode(message: QueryPendingSecretsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPendingSecretsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPendingSecretsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPendingSecretsRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryPendingSecretsRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryPendingSecretsRequest>, I>>(base?: I): QueryPendingSecretsRequest {
    return QueryPendingSecretsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryPendingSecretsRequest>, I>>(object: I): QueryPendingSecretsRequest {
    const message = createBaseQueryPendingSecretsRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryPendingSecretsResponse(): QueryPendingSecretsResponse {
  return { secrets: [], pagination: undefined };
}

export const QueryPendingSecretsResponse: MessageFns<QueryPendingSecretsResponse> = {
  encode(message: QueryPendingSecretsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.secrets) {
      SecretView.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPendingSecretsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPendingSecretsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secrets.push(SecretView.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPendingSecretsResponse {
    return {
      secrets: globalThis.Array.isArray(object?.secrets) ? object.secrets.map((e: any) => SecretView.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryPendingSecretsResponse): unknown {
    const obj: any = {};
    if (message.secrets?.length) {
      obj.secrets = message.secrets.map((e) => SecretView.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryPendingSecretsResponse>, I>>(base?: I): QueryPendingSecretsResponse {
    return QueryPendingSecretsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryPendingSecretsResponse>, I>>(object: I): QueryPendingSecretsResponse {
    const message = createBaseQueryPendingSecretsResponse();
    message.secrets = object.secrets?.map((e) => SecretView.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretMetaRequest(): QuerySecretMetaRequest {
  return { secretId: "" };
}

export const QuerySecretMetaRequest: MessageFns<QuerySecretMetaRequest> = {
  encode(message: QuerySecretMetaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretMetaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretMetaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretMetaRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
    };
  },

  toJSON(message: QuerySecretMetaRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretMetaRequest>, I>>(base?: I): QuerySecretMetaRequest {
    return QuerySecretMetaRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretMetaRequest>, I>>(object: I): QuerySecretMetaRequest {
    const message = createBaseQuerySecretMetaRequest();
    message.secretId = object.secretId ?? "";
    return message;
  },
};

function createBaseQuerySecretMetaResponse(): QuerySecretMetaResponse {
  return { secret: undefined };
}

export const QuerySecretMetaResponse: MessageFns<QuerySecretMetaResponse> = {
  encode(message: QuerySecretMetaResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secret !== undefined) {
      Secret.encode(message.secret, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretMetaResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretMetaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secret = Secret.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretMetaResponse {
    return { secret: isSet(object.secret) ? Secret.fromJSON(object.secret) : undefined };
  },

  toJSON(message: QuerySecretMetaResponse): unknown {
    const obj: any = {};
    if (message.secret !== undefined) {
      obj.secret = Secret.toJSON(message.secret);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretMetaResponse>, I>>(base?: I): QuerySecretMetaResponse {
    return QuerySecretMetaResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretMetaResponse>, I>>(object: I): QuerySecretMetaResponse {
    const message = createBaseQuerySecretMetaResponse();
    message.secret = (object.secret !== undefined && object.secret !== null)
      ? Secret.fromPartial(object.secret)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretAssignmentsRequest(): QuerySecretAssignmentsRequest {
  return { secretId: "" };
}

export const QuerySecretAssignmentsRequest: MessageFns<QuerySecretAssignmentsRequest> = {
  encode(message: QuerySecretAssignmentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretAssignmentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretAssignmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretAssignmentsRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
    };
  },

  toJSON(message: QuerySecretAssignmentsRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretAssignmentsRequest>, I>>(base?: I): QuerySecretAssignmentsRequest {
    return QuerySecretAssignmentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretAssignmentsRequest>, I>>(
    object: I,
  ): QuerySecretAssignmentsRequest {
    const message = createBaseQuerySecretAssignmentsRequest();
    message.secretId = object.secretId ?? "";
    return message;
  },
};

function createBaseQuerySecretAssignmentsResponse(): QuerySecretAssignmentsResponse {
  return { assignments: [] };
}

export const QuerySecretAssignmentsResponse: MessageFns<QuerySecretAssignmentsResponse> = {
  encode(message: QuerySecretAssignmentsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.assignments) {
      SecretAssignmentView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretAssignmentsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretAssignmentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assignments.push(SecretAssignmentView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretAssignmentsResponse {
    return {
      assignments: globalThis.Array.isArray(object?.assignments)
        ? object.assignments.map((e: any) => SecretAssignmentView.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QuerySecretAssignmentsResponse): unknown {
    const obj: any = {};
    if (message.assignments?.length) {
      obj.assignments = message.assignments.map((e) => SecretAssignmentView.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretAssignmentsResponse>, I>>(base?: I): QuerySecretAssignmentsResponse {
    return QuerySecretAssignmentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretAssignmentsResponse>, I>>(
    object: I,
  ): QuerySecretAssignmentsResponse {
    const message = createBaseQuerySecretAssignmentsResponse();
    message.assignments = object.assignments?.map((e) => SecretAssignmentView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQuerySecretRevealsRequest(): QuerySecretRevealsRequest {
  return { secretId: "" };
}

export const QuerySecretRevealsRequest: MessageFns<QuerySecretRevealsRequest> = {
  encode(message: QuerySecretRevealsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretRevealsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretRevealsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretRevealsRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
    };
  },

  toJSON(message: QuerySecretRevealsRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretRevealsRequest>, I>>(base?: I): QuerySecretRevealsRequest {
    return QuerySecretRevealsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretRevealsRequest>, I>>(object: I): QuerySecretRevealsRequest {
    const message = createBaseQuerySecretRevealsRequest();
    message.secretId = object.secretId ?? "";
    return message;
  },
};

function createBaseQuerySecretRevealsResponse(): QuerySecretRevealsResponse {
  return { reveals: [] };
}

export const QuerySecretRevealsResponse: MessageFns<QuerySecretRevealsResponse> = {
  encode(message: QuerySecretRevealsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.reveals) {
      RevealedShare.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretRevealsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretRevealsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reveals.push(RevealedShare.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretRevealsResponse {
    return {
      reveals: globalThis.Array.isArray(object?.reveals)
        ? object.reveals.map((e: any) => RevealedShare.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QuerySecretRevealsResponse): unknown {
    const obj: any = {};
    if (message.reveals?.length) {
      obj.reveals = message.reveals.map((e) => RevealedShare.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretRevealsResponse>, I>>(base?: I): QuerySecretRevealsResponse {
    return QuerySecretRevealsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretRevealsResponse>, I>>(object: I): QuerySecretRevealsResponse {
    const message = createBaseQuerySecretRevealsResponse();
    message.reveals = object.reveals?.map((e) => RevealedShare.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQuerySecretShareRequest(): QuerySecretShareRequest {
  return { secretId: "", guardianAddress: "" };
}

export const QuerySecretShareRequest: MessageFns<QuerySecretShareRequest> = {
  encode(message: QuerySecretShareRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.guardianAddress !== "") {
      writer.uint32(18).string(message.guardianAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretShareRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretShareRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guardianAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretShareRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      guardianAddress: isSet(object.guardianAddress)
        ? globalThis.String(object.guardianAddress)
        : isSet(object.guardian_address)
        ? globalThis.String(object.guardian_address)
        : "",
    };
  },

  toJSON(message: QuerySecretShareRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.guardianAddress !== "") {
      obj.guardianAddress = message.guardianAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretShareRequest>, I>>(base?: I): QuerySecretShareRequest {
    return QuerySecretShareRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretShareRequest>, I>>(object: I): QuerySecretShareRequest {
    const message = createBaseQuerySecretShareRequest();
    message.secretId = object.secretId ?? "";
    message.guardianAddress = object.guardianAddress ?? "";
    return message;
  },
};

function createBaseQuerySecretShareResponse(): QuerySecretShareResponse {
  return { share: undefined };
}

export const QuerySecretShareResponse: MessageFns<QuerySecretShareResponse> = {
  encode(message: QuerySecretShareResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.share !== undefined) {
      SecretShareData.encode(message.share, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretShareResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretShareResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.share = SecretShareData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretShareResponse {
    return { share: isSet(object.share) ? SecretShareData.fromJSON(object.share) : undefined };
  },

  toJSON(message: QuerySecretShareResponse): unknown {
    const obj: any = {};
    if (message.share !== undefined) {
      obj.share = SecretShareData.toJSON(message.share);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretShareResponse>, I>>(base?: I): QuerySecretShareResponse {
    return QuerySecretShareResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretShareResponse>, I>>(object: I): QuerySecretShareResponse {
    const message = createBaseQuerySecretShareResponse();
    message.share = (object.share !== undefined && object.share !== null)
      ? SecretShareData.fromPartial(object.share)
      : undefined;
    return message;
  },
};

function createBaseQuerySecretPayloadRequest(): QuerySecretPayloadRequest {
  return { secretId: "" };
}

export const QuerySecretPayloadRequest: MessageFns<QuerySecretPayloadRequest> = {
  encode(message: QuerySecretPayloadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretPayloadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretPayloadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretPayloadRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
    };
  },

  toJSON(message: QuerySecretPayloadRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretPayloadRequest>, I>>(base?: I): QuerySecretPayloadRequest {
    return QuerySecretPayloadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretPayloadRequest>, I>>(object: I): QuerySecretPayloadRequest {
    const message = createBaseQuerySecretPayloadRequest();
    message.secretId = object.secretId ?? "";
    return message;
  },
};

function createBaseQuerySecretPayloadResponse(): QuerySecretPayloadResponse {
  return { payloadCiphertext: new Uint8Array(0) };
}

export const QuerySecretPayloadResponse: MessageFns<QuerySecretPayloadResponse> = {
  encode(message: QuerySecretPayloadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payloadCiphertext.length !== 0) {
      writer.uint32(10).bytes(message.payloadCiphertext);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretPayloadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretPayloadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payloadCiphertext = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretPayloadResponse {
    return {
      payloadCiphertext: isSet(object.payloadCiphertext)
        ? bytesFromBase64(object.payloadCiphertext)
        : isSet(object.payload_ciphertext)
        ? bytesFromBase64(object.payload_ciphertext)
        : new Uint8Array(0),
    };
  },

  toJSON(message: QuerySecretPayloadResponse): unknown {
    const obj: any = {};
    if (message.payloadCiphertext.length !== 0) {
      obj.payloadCiphertext = base64FromBytes(message.payloadCiphertext);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretPayloadResponse>, I>>(base?: I): QuerySecretPayloadResponse {
    return QuerySecretPayloadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretPayloadResponse>, I>>(object: I): QuerySecretPayloadResponse {
    const message = createBaseQuerySecretPayloadResponse();
    message.payloadCiphertext = object.payloadCiphertext ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQuerySecretTombstoneRequest(): QuerySecretTombstoneRequest {
  return { secretId: "" };
}

export const QuerySecretTombstoneRequest: MessageFns<QuerySecretTombstoneRequest> = {
  encode(message: QuerySecretTombstoneRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretTombstoneRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretTombstoneRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretTombstoneRequest {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
    };
  },

  toJSON(message: QuerySecretTombstoneRequest): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretTombstoneRequest>, I>>(base?: I): QuerySecretTombstoneRequest {
    return QuerySecretTombstoneRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretTombstoneRequest>, I>>(object: I): QuerySecretTombstoneRequest {
    const message = createBaseQuerySecretTombstoneRequest();
    message.secretId = object.secretId ?? "";
    return message;
  },
};

function createBaseQuerySecretTombstoneResponse(): QuerySecretTombstoneResponse {
  return { tombstone: undefined };
}

export const QuerySecretTombstoneResponse: MessageFns<QuerySecretTombstoneResponse> = {
  encode(message: QuerySecretTombstoneResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tombstone !== undefined) {
      SecretTombstone.encode(message.tombstone, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySecretTombstoneResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySecretTombstoneResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tombstone = SecretTombstone.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySecretTombstoneResponse {
    return { tombstone: isSet(object.tombstone) ? SecretTombstone.fromJSON(object.tombstone) : undefined };
  },

  toJSON(message: QuerySecretTombstoneResponse): unknown {
    const obj: any = {};
    if (message.tombstone !== undefined) {
      obj.tombstone = SecretTombstone.toJSON(message.tombstone);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuerySecretTombstoneResponse>, I>>(base?: I): QuerySecretTombstoneResponse {
    return QuerySecretTombstoneResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuerySecretTombstoneResponse>, I>>(object: I): QuerySecretTombstoneResponse {
    const message = createBaseQuerySecretTombstoneResponse();
    message.tombstone = (object.tombstone !== undefined && object.tombstone !== null)
      ? SecretTombstone.fromPartial(object.tombstone)
      : undefined;
    return message;
  },
};

function createBaseQueryHintsSinceRequest(): QueryHintsSinceRequest {
  return { sinceHeight: 0, pagination: undefined };
}

export const QueryHintsSinceRequest: MessageFns<QueryHintsSinceRequest> = {
  encode(message: QueryHintsSinceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sinceHeight !== 0) {
      writer.uint32(8).int64(message.sinceHeight);
    }
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryHintsSinceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHintsSinceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sinceHeight = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHintsSinceRequest {
    return {
      sinceHeight: isSet(object.sinceHeight)
        ? globalThis.Number(object.sinceHeight)
        : isSet(object.since_height)
        ? globalThis.Number(object.since_height)
        : 0,
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryHintsSinceRequest): unknown {
    const obj: any = {};
    if (message.sinceHeight !== 0) {
      obj.sinceHeight = Math.round(message.sinceHeight);
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryHintsSinceRequest>, I>>(base?: I): QueryHintsSinceRequest {
    return QueryHintsSinceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryHintsSinceRequest>, I>>(object: I): QueryHintsSinceRequest {
    const message = createBaseQueryHintsSinceRequest();
    message.sinceHeight = object.sinceHeight ?? 0;
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseHintRecord(): HintRecord {
  return { secretId: "", createdAt: 0, detectionHint: undefined };
}

export const HintRecord: MessageFns<HintRecord> = {
  encode(message: HintRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretId !== "") {
      writer.uint32(10).string(message.secretId);
    }
    if (message.createdAt !== 0) {
      writer.uint32(16).int64(message.createdAt);
    }
    if (message.detectionHint !== undefined) {
      DetectionHint.encode(message.detectionHint, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HintRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHintRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.detectionHint = DetectionHint.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HintRecord {
    return {
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.Number(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.Number(object.created_at)
        : 0,
      detectionHint: isSet(object.detectionHint)
        ? DetectionHint.fromJSON(object.detectionHint)
        : isSet(object.detection_hint)
        ? DetectionHint.fromJSON(object.detection_hint)
        : undefined,
    };
  },

  toJSON(message: HintRecord): unknown {
    const obj: any = {};
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.detectionHint !== undefined) {
      obj.detectionHint = DetectionHint.toJSON(message.detectionHint);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HintRecord>, I>>(base?: I): HintRecord {
    return HintRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HintRecord>, I>>(object: I): HintRecord {
    const message = createBaseHintRecord();
    message.secretId = object.secretId ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.detectionHint = (object.detectionHint !== undefined && object.detectionHint !== null)
      ? DetectionHint.fromPartial(object.detectionHint)
      : undefined;
    return message;
  },
};

function createBaseQueryHintsSinceResponse(): QueryHintsSinceResponse {
  return { hints: [], pagination: undefined };
}

export const QueryHintsSinceResponse: MessageFns<QueryHintsSinceResponse> = {
  encode(message: QueryHintsSinceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hints) {
      HintRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryHintsSinceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHintsSinceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hints.push(HintRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHintsSinceResponse {
    return {
      hints: globalThis.Array.isArray(object?.hints) ? object.hints.map((e: any) => HintRecord.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryHintsSinceResponse): unknown {
    const obj: any = {};
    if (message.hints?.length) {
      obj.hints = message.hints.map((e) => HintRecord.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryHintsSinceResponse>, I>>(base?: I): QueryHintsSinceResponse {
    return QueryHintsSinceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryHintsSinceResponse>, I>>(object: I): QueryHintsSinceResponse {
    const message = createBaseQueryHintsSinceResponse();
    message.hints = object.hints?.map((e) => HintRecord.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryGuardianRequest(): QueryGuardianRequest {
  return { address: "" };
}

export const QueryGuardianRequest: MessageFns<QueryGuardianRequest> = {
  encode(message: QueryGuardianRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGuardianRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGuardianRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGuardianRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryGuardianRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGuardianRequest>, I>>(base?: I): QueryGuardianRequest {
    return QueryGuardianRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGuardianRequest>, I>>(object: I): QueryGuardianRequest {
    const message = createBaseQueryGuardianRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryGuardianResponse(): QueryGuardianResponse {
  return { guardian: undefined };
}

export const QueryGuardianResponse: MessageFns<QueryGuardianResponse> = {
  encode(message: QueryGuardianResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guardian !== undefined) {
      Guardian.encode(message.guardian, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGuardianResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGuardianResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guardian = Guardian.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGuardianResponse {
    return { guardian: isSet(object.guardian) ? Guardian.fromJSON(object.guardian) : undefined };
  },

  toJSON(message: QueryGuardianResponse): unknown {
    const obj: any = {};
    if (message.guardian !== undefined) {
      obj.guardian = Guardian.toJSON(message.guardian);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGuardianResponse>, I>>(base?: I): QueryGuardianResponse {
    return QueryGuardianResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGuardianResponse>, I>>(object: I): QueryGuardianResponse {
    const message = createBaseQueryGuardianResponse();
    message.guardian = (object.guardian !== undefined && object.guardian !== null)
      ? Guardian.fromPartial(object.guardian)
      : undefined;
    return message;
  },
};

function createBaseQueryGuardiansRequest(): QueryGuardiansRequest {
  return { pagination: undefined };
}

export const QueryGuardiansRequest: MessageFns<QueryGuardiansRequest> = {
  encode(message: QueryGuardiansRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGuardiansRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGuardiansRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGuardiansRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryGuardiansRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGuardiansRequest>, I>>(base?: I): QueryGuardiansRequest {
    return QueryGuardiansRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGuardiansRequest>, I>>(object: I): QueryGuardiansRequest {
    const message = createBaseQueryGuardiansRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryGuardiansResponse(): QueryGuardiansResponse {
  return { guardians: [], pagination: undefined };
}

export const QueryGuardiansResponse: MessageFns<QueryGuardiansResponse> = {
  encode(message: QueryGuardiansResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.guardians) {
      Guardian.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGuardiansResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGuardiansResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guardians.push(Guardian.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGuardiansResponse {
    return {
      guardians: globalThis.Array.isArray(object?.guardians)
        ? object.guardians.map((e: any) => Guardian.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryGuardiansResponse): unknown {
    const obj: any = {};
    if (message.guardians?.length) {
      obj.guardians = message.guardians.map((e) => Guardian.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGuardiansResponse>, I>>(base?: I): QueryGuardiansResponse {
    return QueryGuardiansResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGuardiansResponse>, I>>(object: I): QueryGuardiansResponse {
    const message = createBaseQueryGuardiansResponse();
    message.guardians = object.guardians?.map((e) => Guardian.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseGuardianKeyEpoch(): GuardianKeyEpoch {
  return { epoch: 0, entry: undefined };
}

export const GuardianKeyEpoch: MessageFns<GuardianKeyEpoch> = {
  encode(message: GuardianKeyEpoch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.epoch !== 0) {
      writer.uint32(8).uint64(message.epoch);
    }
    if (message.entry !== undefined) {
      KeyHistoryEntry.encode(message.entry, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GuardianKeyEpoch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGuardianKeyEpoch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.epoch = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entry = KeyHistoryEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GuardianKeyEpoch {
    return {
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      entry: isSet(object.entry) ? KeyHistoryEntry.fromJSON(object.entry) : undefined,
    };
  },

  toJSON(message: GuardianKeyEpoch): unknown {
    const obj: any = {};
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.entry !== undefined) {
      obj.entry = KeyHistoryEntry.toJSON(message.entry);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GuardianKeyEpoch>, I>>(base?: I): GuardianKeyEpoch {
    return GuardianKeyEpoch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GuardianKeyEpoch>, I>>(object: I): GuardianKeyEpoch {
    const message = createBaseGuardianKeyEpoch();
    message.epoch = object.epoch ?? 0;
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? KeyHistoryEntry.fromPartial(object.entry)
      : undefined;
    return message;
  },
};

function createBaseQueryGuardianKeyHistoryRequest(): QueryGuardianKeyHistoryRequest {
  return { address: "" };
}

export const QueryGuardianKeyHistoryRequest: MessageFns<QueryGuardianKeyHistoryRequest> = {
  encode(message: QueryGuardianKeyHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGuardianKeyHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGuardianKeyHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGuardianKeyHistoryRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryGuardianKeyHistoryRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGuardianKeyHistoryRequest>, I>>(base?: I): QueryGuardianKeyHistoryRequest {
    return QueryGuardianKeyHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGuardianKeyHistoryRequest>, I>>(
    object: I,
  ): QueryGuardianKeyHistoryRequest {
    const message = createBaseQueryGuardianKeyHistoryRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryGuardianKeyHistoryResponse(): QueryGuardianKeyHistoryResponse {
  return { epochs: [] };
}

export const QueryGuardianKeyHistoryResponse: MessageFns<QueryGuardianKeyHistoryResponse> = {
  encode(message: QueryGuardianKeyHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.epochs) {
      GuardianKeyEpoch.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGuardianKeyHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGuardianKeyHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.epochs.push(GuardianKeyEpoch.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGuardianKeyHistoryResponse {
    return {
      epochs: globalThis.Array.isArray(object?.epochs)
        ? object.epochs.map((e: any) => GuardianKeyEpoch.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryGuardianKeyHistoryResponse): unknown {
    const obj: any = {};
    if (message.epochs?.length) {
      obj.epochs = message.epochs.map((e) => GuardianKeyEpoch.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGuardianKeyHistoryResponse>, I>>(base?: I): QueryGuardianKeyHistoryResponse {
    return QueryGuardianKeyHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGuardianKeyHistoryResponse>, I>>(
    object: I,
  ): QueryGuardianKeyHistoryResponse {
    const message = createBaseQueryGuardianKeyHistoryResponse();
    message.epochs = object.epochs?.map((e) => GuardianKeyEpoch.fromPartial(e)) || [];
    return message;
  },
};

/** Query defines the gRPC querier service. */
export interface Query {
  /**
   * Secret queries a secret by ID, assembled with its guardian assignments
   * and revealed shares (the full pre-split wire shape).
   */
  Secret(request: QuerySecretRequest): Promise<QuerySecretResponse>;
  /** Secrets queries all secrets (assembled views) with pagination. */
  Secrets(request: QuerySecretsRequest): Promise<QuerySecretsResponse>;
  /** SecretsByCreator queries secrets by creator address (assembled views). */
  SecretsByCreator(request: QuerySecretsByCreatorRequest): Promise<QuerySecretsByCreatorResponse>;
  /**
   * PendingSecrets queries secrets in their active reveal phase
   * (pending/reconstructable), as assembled views.
   */
  PendingSecrets(request: QueryPendingSecretsRequest): Promise<QueryPendingSecretsResponse>;
  /**
   * SecretMeta queries only the slim secret metadata record — no share or
   * reveal data. The cheap call for light clients.
   */
  SecretMeta(request: QuerySecretMetaRequest): Promise<QuerySecretMetaResponse>;
  /**
   * SecretAssignments queries the per-guardian assignment status records for
   * a secret (tiny; no share bytes).
   */
  SecretAssignments(request: QuerySecretAssignmentsRequest): Promise<QuerySecretAssignmentsResponse>;
  /**
   * SecretReveals queries the revealed shares for a secret (what a recipient
   * needs to reconstruct).
   */
  SecretReveals(request: QuerySecretRevealsRequest): Promise<QuerySecretRevealsResponse>;
  /**
   * SecretShare queries a single guardian's encrypted share for a secret —
   * what a guardian daemon needs, without the other guardians' shares.
   */
  SecretShare(request: QuerySecretShareRequest): Promise<QuerySecretShareResponse>;
  /**
   * SecretPayload queries a secret's stored payload ciphertext (C) — what a
   * reconstruction client needs alongside the revealed key shares.
   */
  SecretPayload(request: QuerySecretPayloadRequest): Promise<QuerySecretPayloadResponse>;
  /**
   * HintsSince returns compact (secret_id, detection_hint) tuples for secrets
   * created at or after the given height, in creation order — the incremental
   * discovery-scan cursor. Recipients poll this, test each hint against their
   * private key client-side, and resume from their last scanned height.
   * A pruned secret's hint is deleted at Stage 2 (retention): discovery is
   * bounded by the retention window by design — scan at least every six months.
   */
  HintsSince(request: QueryHintsSinceRequest): Promise<QueryHintsSinceResponse>;
  /**
   * SecretTombstone returns the permanent tombstone of a pruned secret —
   * distinguishing "pruned" (archived, digest-verifiable) from "never
   * existed" (NotFound on both this and Query/Secret).
   */
  SecretTombstone(request: QuerySecretTombstoneRequest): Promise<QuerySecretTombstoneResponse>;
  /** Guardian queries a guardian by address. */
  Guardian(request: QueryGuardianRequest): Promise<QueryGuardianResponse>;
  /** Guardians queries all guardians with pagination. */
  Guardians(request: QueryGuardiansRequest): Promise<QueryGuardiansResponse>;
  /**
   * GuardianKeyHistory queries a guardian's append-only key-epoch history in
   * epoch order — what a daemon or client needs to resolve the epoch in
   * force at any height (newest entry with effective_from_height <= h).
   */
  GuardianKeyHistory(request: QueryGuardianKeyHistoryRequest): Promise<QueryGuardianKeyHistoryResponse>;
}

export const QueryServiceName = "timeflare.secrets.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Secret = this.Secret.bind(this);
    this.Secrets = this.Secrets.bind(this);
    this.SecretsByCreator = this.SecretsByCreator.bind(this);
    this.PendingSecrets = this.PendingSecrets.bind(this);
    this.SecretMeta = this.SecretMeta.bind(this);
    this.SecretAssignments = this.SecretAssignments.bind(this);
    this.SecretReveals = this.SecretReveals.bind(this);
    this.SecretShare = this.SecretShare.bind(this);
    this.SecretPayload = this.SecretPayload.bind(this);
    this.HintsSince = this.HintsSince.bind(this);
    this.SecretTombstone = this.SecretTombstone.bind(this);
    this.Guardian = this.Guardian.bind(this);
    this.Guardians = this.Guardians.bind(this);
    this.GuardianKeyHistory = this.GuardianKeyHistory.bind(this);
  }
  Secret(request: QuerySecretRequest): Promise<QuerySecretResponse> {
    const data = QuerySecretRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Secret", data);
    return promise.then((data) => QuerySecretResponse.decode(new BinaryReader(data)));
  }

  Secrets(request: QuerySecretsRequest): Promise<QuerySecretsResponse> {
    const data = QuerySecretsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Secrets", data);
    return promise.then((data) => QuerySecretsResponse.decode(new BinaryReader(data)));
  }

  SecretsByCreator(request: QuerySecretsByCreatorRequest): Promise<QuerySecretsByCreatorResponse> {
    const data = QuerySecretsByCreatorRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretsByCreator", data);
    return promise.then((data) => QuerySecretsByCreatorResponse.decode(new BinaryReader(data)));
  }

  PendingSecrets(request: QueryPendingSecretsRequest): Promise<QueryPendingSecretsResponse> {
    const data = QueryPendingSecretsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "PendingSecrets", data);
    return promise.then((data) => QueryPendingSecretsResponse.decode(new BinaryReader(data)));
  }

  SecretMeta(request: QuerySecretMetaRequest): Promise<QuerySecretMetaResponse> {
    const data = QuerySecretMetaRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretMeta", data);
    return promise.then((data) => QuerySecretMetaResponse.decode(new BinaryReader(data)));
  }

  SecretAssignments(request: QuerySecretAssignmentsRequest): Promise<QuerySecretAssignmentsResponse> {
    const data = QuerySecretAssignmentsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretAssignments", data);
    return promise.then((data) => QuerySecretAssignmentsResponse.decode(new BinaryReader(data)));
  }

  SecretReveals(request: QuerySecretRevealsRequest): Promise<QuerySecretRevealsResponse> {
    const data = QuerySecretRevealsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretReveals", data);
    return promise.then((data) => QuerySecretRevealsResponse.decode(new BinaryReader(data)));
  }

  SecretShare(request: QuerySecretShareRequest): Promise<QuerySecretShareResponse> {
    const data = QuerySecretShareRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretShare", data);
    return promise.then((data) => QuerySecretShareResponse.decode(new BinaryReader(data)));
  }

  SecretPayload(request: QuerySecretPayloadRequest): Promise<QuerySecretPayloadResponse> {
    const data = QuerySecretPayloadRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretPayload", data);
    return promise.then((data) => QuerySecretPayloadResponse.decode(new BinaryReader(data)));
  }

  HintsSince(request: QueryHintsSinceRequest): Promise<QueryHintsSinceResponse> {
    const data = QueryHintsSinceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "HintsSince", data);
    return promise.then((data) => QueryHintsSinceResponse.decode(new BinaryReader(data)));
  }

  SecretTombstone(request: QuerySecretTombstoneRequest): Promise<QuerySecretTombstoneResponse> {
    const data = QuerySecretTombstoneRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SecretTombstone", data);
    return promise.then((data) => QuerySecretTombstoneResponse.decode(new BinaryReader(data)));
  }

  Guardian(request: QueryGuardianRequest): Promise<QueryGuardianResponse> {
    const data = QueryGuardianRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Guardian", data);
    return promise.then((data) => QueryGuardianResponse.decode(new BinaryReader(data)));
  }

  Guardians(request: QueryGuardiansRequest): Promise<QueryGuardiansResponse> {
    const data = QueryGuardiansRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Guardians", data);
    return promise.then((data) => QueryGuardiansResponse.decode(new BinaryReader(data)));
  }

  GuardianKeyHistory(request: QueryGuardianKeyHistoryRequest): Promise<QueryGuardianKeyHistoryResponse> {
    const data = QueryGuardianKeyHistoryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GuardianKeyHistory", data);
    return promise.then((data) => QueryGuardianKeyHistoryResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
